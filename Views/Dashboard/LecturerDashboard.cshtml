@{
    ViewData["Title"] = "Lecturer Dashboard";
}

<div class="container">
    <h2>Lecturer Dashboard</h2>
    <div class="row">
        <div class="col-md-6">
            <a href="@Url.Action("SubmitClaim", "Dashboard")" class="btn btn-primary">Submit New Claim</a>
        </div>
        <div class="col-md-6">
            <a href="@Url.Action("ViewClaimsHistory", "Dashboard")" class="btn btn-secondary">View Claims History</a>
        </div>
    </div>
    <br />
    <a href="@Url.Action("Logout", "Account")" class="btn btn-danger">Logout</a>
</div>
